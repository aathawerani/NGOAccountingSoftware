<?xml version="1.0" encoding="utf-8"?>
<UserControl xmlns:ui="clr-namespace:TrustApplication.ViewModels" x:Class="TrustApplication.Views.ImportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200"
             UseLayoutRounding="True"
             SnapsToDevicePixels="True">
    <UserControl.Resources>
        <Style x:Key="LabelText" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
        </Style>
        <Style x:Key="Text" TargetType="TextBox">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
        </Style>
        <Style x:Key="Date" TargetType="DatePicker">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
        </Style>
        <Style x:Key="Combo" TargetType="ComboBox">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
        </Style>
        <Style x:Key="BarButton" TargetType="Button">
            <Setter Property="Margin" Value="0,0,8,0"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="MinWidth" Value="90"/>
        </Style>
    </UserControl.Resources>
    <!-- Migrated from Tab 'Import' -->
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <DockPanel LastChildFill="True" Margin="16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <WrapPanel Grid.Row="0" Grid.Column="0" Margin="0,0,8,0">
                    <Grid Width="500" Margin="6">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Column="0" Text="User Given Name *" Style="{StaticResource LabelText}"/>
                        <TextBox Width="350"  Grid.Column="0" Margin="130,0,0,0" x:Name="TxtUserGivenName" Style="{StaticResource Text}"/>
                    </Grid>
                    <Grid Width="600" Margin="6">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Column="0" Text="Excel File (.xlsx) *" Style="{StaticResource LabelText}"/>
                        <TextBox Width="380" Grid.Column="0" Margin="110,0,0,0" x:Name="TxtFilePath" IsReadOnly="True" Style="{StaticResource Text}"/>
                        <Button  Width="80" Grid.Column="0" Margin="500,0,0,0" x:Name="BtnBrowse" Content="Browse" Style="{StaticResource BarButton}" Click="BtnBrowse_Click"/>
                    </Grid>
                    <Grid Width="300" Margin="6">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <CheckBox  Grid.Column="0" x:Name="ChkHasHeader" IsChecked="True">First row contains headers</CheckBox>
                    </Grid>
                    <Grid Width="1100" Margin="6">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <TextBox Grid.Column="0" Grid.ColumnSpan="3" x:Name="TxtLog"
                             Height="220" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                             TextWrapping="Wrap" IsReadOnly="True" />
                    </Grid>
                    <Grid Width="900" Margin="6">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <StackPanel Grid.Column="0" Grid.ColumnSpan="3" Orientation="Horizontal" HorizontalAlignment="left">
                            <ProgressBar x:Name="Progress" Width="750" Height="18" Minimum="0" Maximum="100" />
                            <Button x:Name="BtnImport" Margin="10,0,0,0" Content="Import" Style="{StaticResource BarButton}" Click="BtnImport_Click"/>
                        </StackPanel>
                    </Grid>
                </WrapPanel>
            </Grid>
        </DockPanel>
    </ScrollViewer>
</UserControl>
